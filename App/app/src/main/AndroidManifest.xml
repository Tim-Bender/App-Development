<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.Spudnik">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".LoginActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".BluetoothTestActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <service android:name=".BluetoothLeService" />

        <activity android:name=".settings"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".pinlocation"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Pindiagnostic"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".selectpin"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".connectorselect"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".inputserial"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>